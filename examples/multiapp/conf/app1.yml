app:
  component: 'app1'
  data:
    sources:
        # Set here references from sources defined on data.sources
        - 'session'
  pythonpath: '..'
  #type: 'tornado'
  port: 8888
  process:
    num_processes: 0
